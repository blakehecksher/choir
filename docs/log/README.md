# Log

Append-only history. One file per session.

Naming: `YYYY-MM-DD HHMM Subject.md` — get the timestamp from `date '+%Y-%m-%d %H%M'`.

Don't read these unless `docs/state.md` links you here. Every log must start with a TL;DR section.
